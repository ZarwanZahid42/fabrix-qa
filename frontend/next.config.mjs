// Purpose: configure the Next.js website as implementation requirements emerge.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
